{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: CurrentFile",
            "type": "python",
            "envFile": "${workspaceFolder}/.env",
            "console": "integratedTerminal"
        }
    ]
}